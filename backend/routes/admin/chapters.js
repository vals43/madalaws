const express = require('express'); const { authenticateAdmin } = require('../../middleware/auth'); const ctrl = require('../../controllers/admin/chaptersController'); const router = express.Router(); router.use(authenticateAdmin); router.post('/', ctrl.create); router.put('/:id', ctrl.update); router.delete('/:id', ctrl.delete); module.exports = router;